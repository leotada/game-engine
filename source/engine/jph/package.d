// Jolt Physics — D port. Top-level re-export.
//
// Mirrors the JPH:: namespace as the `engine.jph.*` package. Replaces the
// previous engine.physics implementation (deleted; preserved on the
// pre-jph-port branch). Source of truth: ref/JoltPhysics/Jolt/.
module engine.jph;

public import engine.jph.core;
public import engine.jph.math;
public import engine.jph.geometry;
public import engine.jph.physics;
