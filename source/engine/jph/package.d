// Jolt Physics — D port. Top-level re-export.
//
// Mirrors the JPH:: namespace as the `engine.jph.*` package. The faithful
// conversion lives here in parallel with the existing engine.physics
// implementation; nothing in this package is wired into the runtime yet.
//
// Source of truth: ref/JoltPhysics/Jolt/.
module engine.jph;

public import engine.jph.core.types;
public import engine.jph.math;
