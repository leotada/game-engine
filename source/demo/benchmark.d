// Phase-0 stub: original benchmark scene (forest + rain) lives under
// source/demo/_legacy/benchmark.d.txt and will be restored in Phase 9 once
// engine.jph.physics is in place. Until then this prints a notice and exits.
module demo.benchmark;

import std.stdio : writeln;

int main() {
    writeln("[benchmark] engine.jph rigid-body port in progress.");
    writeln("[benchmark] Original scene preserved at source/demo/_legacy/benchmark.d.txt.");
    return 0;
}
